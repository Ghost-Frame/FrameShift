schema_version = 1
name = "visual-director"
author_handle = "ghost-frame"
author_pubkey = "UNSIGNED"
version = "0.1.0"
description = "Visual briefing, art direction, collection coherence, critique, and deterministic asset QA."
tags = ["visual", "art-direction", "image-generation", "asset-qa"]
license = "Elastic-2.0"

[capability_manifest]
required_tools = ["Read", "Edit", "Write", "Bash", "Grep", "Glob"]
network_egress = true
filesystem_scope = "project-only"
memory_required = "soft"
memory_required_ops = ["search", "store"]
