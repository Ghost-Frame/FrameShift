schema_version = 1
name = "systems"
author_handle = "ghost-frame"
author_pubkey = "UNSIGNED"
version = "0.1.0"
description = "Systems engineering across machines, services, shells, and operations."
tags = ["systems", "operations", "shell", "services"]
license = "Elastic-2.0"

[capability_manifest]
required_tools = ["Read", "Edit", "Write", "Bash", "Grep", "Glob"]
network_egress = true
filesystem_scope = "home"
memory_required = "none"
memory_required_ops = []

# Signed, tamper-evident behavioral claim. bundle_hash is the sha256 of the
# canonical TOML for this persona's private conformance bundle
# (personas-private/systems/conformance/bundle.toml, 5 regex cases:
# state-check-before-change, never-reboot-OVH, ssh-as-zan-via-alias,
# classify-blast-radius, heredoc-truncates-use-scp). The bundle is not shipped
# in the manifest-only public catalog; this claim travels with the pack and the
# regression gate rejects a version bump whose bundle no longer hashes to this.
# score is the asserted floor, measured once a non-mock Runner lands.
[conformance_baseline]
score = 0.8
bundle_hash = "a837149d20ae446ec770617ac5fea4a7f8db5e9b4bd29d4d28499c46f875051a"
