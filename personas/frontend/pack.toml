schema_version = 1
name = "frontend"
author_handle = "ghost-frame"
author_pubkey = "UNSIGNED"
version = "0.1.0"
description = "Web UI implementation, interaction design, and product polish."
tags = ["frontend", "ui", "ux", "web"]
license = "Elastic-2.0"

[capability_manifest]
required_tools = ["Read", "Edit", "Write", "Bash", "Grep", "Glob"]
network_egress = false
filesystem_scope = "project-only"
memory_required = "none"
memory_required_ops = []
