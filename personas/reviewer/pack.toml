schema_version = 1
name = "reviewer"
author_handle = "ghost-frame"
author_pubkey = "UNSIGNED"
version = "0.1.0"
description = "Code review focused on bugs, regressions, and missing tests."
tags = ["review", "bugs", "regressions", "testing"]
license = "Elastic-2.0"

[capability_manifest]
required_tools = ["Read", "Grep", "Glob", "Bash"]
network_egress = false
filesystem_scope = "project-only"
memory_required = "none"
memory_required_ops = []
