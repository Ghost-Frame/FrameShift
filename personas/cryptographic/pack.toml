schema_version = 1
name = "cryptographic"
author_handle = "ghost-frame"
author_pubkey = "UNSIGNED"
version = "0.1.0"
description = "Applied cryptography, key handling, and protocol-level security work."
tags = ["cryptography", "security", "keys", "protocols"]
license = "Elastic-2.0"

[capability_manifest]
required_tools = ["Read", "Edit", "Write", "Bash", "Grep", "Glob"]
network_egress = false
filesystem_scope = "project-only"
memory_required = "none"
memory_required_ops = []

# Conformance baseline: a signed, tamper-evident claim about the persona's
# behavior. `bundle_hash` is the sha256 of the canonical TOML for this persona's
# private conformance bundle (not shipped;
# 6 regex cases covering audited-over-custom, constant-time comparison,
# RustCrypto-not-OpenSSL, OsRng key generation, spec citation, secret wrapping).
# The public catalog is manifest-only, so the bundle itself is not shipped here;
# this claim travels with the pack and the regression gate rejects a version bump
# whose bundle no longer hashes to this value. `score` is the measured floor:
# 1.0 across 2 runs (Gemini 3.1 Pro, 2026-07-07); every case passed.
[conformance_baseline]
score = 1.0
bundle_hash = "990c06b85b1eb295ca8cbb21dc6541d8c4899c334880bd027efd1b588b30ce61"
